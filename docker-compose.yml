services:
  api:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8000:8000"

  worker:
    build:
      context: .
      dockerfile: app/Dockerfile
    command: celery -A app.tasks.transaction worker --loglevel=info
    depends_on:
      - redis

  db:
    image: postgres:18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: transactions
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"
